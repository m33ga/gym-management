<Page x:Class="YourNamespace.Views.MealPlans.MealPlanTrainerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:YourNamespace.Views.MealPlans"
      xmlns:viewModels="using:GymManagement.UWP.ViewModels">

    <Grid Padding="20">
        <StackPanel>
            <TextBlock Text="Meal Plan Creation" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" />
            <ComboBox ItemsSource="{Binding Members}" SelectedItem="{Binding SelectedMember}" PlaceholderText="Select Member" />
            <ComboBox ItemsSource="{Binding Days}" SelectedItem="{Binding SelectedDay}" PlaceholderText="Select Day" />
            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <TextBox Text="{Binding NewDishName}" PlaceholderText="Dish Name" Width="200" Margin="0,0,10,0" />
                <Button Content="Add Dish" Command="{Binding AddDishCommand}" />
            </StackPanel>
            <ListBox ItemsSource="{Binding Dishes}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" />
                            <Button Content="Remove" Command="{Binding RemoveDishCommand}" CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Assign Meal Plan" Command="{Binding AssignMealPlanCommand}" Margin="0,20,0,0" />
        </StackPanel>
    </Grid>
</Page>
